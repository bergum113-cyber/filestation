# api 폴더 직접 접근 차단
# 이 폴더의 PHP 파일들은 include/require로만 사용됨

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
